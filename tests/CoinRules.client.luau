local Rules=require(game.ReplicatedStorage.CoinRules)
assert(Rules.Capacity==15)
assert(Rules.CanToss(15,nil,100))
assert(not Rules.CanToss(15,100,100.5))
assert(Rules.CanToss(14,100,101.11))
assert(not Rules.CanToss(0,nil,100))
assert(Rules.GrabTime<Rules.ReleaseTime and Rules.ReleaseTime<Rules.Duration)
local Config=require(game.ReplicatedStorage.MetalTuningConfig)
assert(Config.AnchorRangeForSize(Rules.Size,750)>0)
print("PASS: finite inventory, nonqueued action timing and sensing range")
